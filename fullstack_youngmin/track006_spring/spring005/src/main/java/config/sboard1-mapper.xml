<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thejoa703.dao.Sboard1Dao">  

      <insert id="insert"  parameterType="Sboard1Dto">
      insert into sboard1 (ID, App_USER_ID, BTITLE, BCONTENT, BPASS)
      values (sboard1_seq.nextval , #{appUserId}, #{btitel},#{bcontent},#{bpass},#{bip})
      </insert>
      
      <update  id="update"  parameterType="Sboard1Dto" >
      update sboard1
       set BTITLE= #{btitel} , BCONTENT=#{bcontent}, BPASS= #{bpass}
       where id=4;
      </update>
      
      <delete id="delete"  parameterType="Sboard1Dto">
      delete from sboard1 where id=#{id} and BPASS= #{bpass}
      </delete>
      
      <select  resultType="Sboard1Dto"  id="selectAll">
      </select>
      
      <select  resultType="Sboard1Dto"  id="select" parameterType="int">
      </select>
      
</mapper>